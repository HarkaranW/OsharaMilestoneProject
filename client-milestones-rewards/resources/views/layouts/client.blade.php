<!DOCTYPE html>
<html>
<head>
    <title>Client Milestones Rewards</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <style>
        body { font-family: sans-serif; background: #f5f6f8; }
        .card { max-width: 600px; margin: 50px auto; background: #fff; padding: 30px; border-radius: 8px; }
        .btn { display: inline-block; padding: 12px 20px; background: #000; color: #fff; text-decoration: none; }
        .muted { color: #666; font-size: 14px; }
        .warning { color: #c0392b; }
        .success { color: #27ae60; }
    </style>
</head>
<body>
    <div class="card">
        @yield('content')
    </div>
</body>
</html>
